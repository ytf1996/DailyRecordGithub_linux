@using MyNetCore.Models
@using MyNetCore.Business
@{
    var htmlStr = BusinessHelper.BuildListPage<Workflow>();
}
@htmlStr


<script>
    tableColumn = [
        //{
        //    checkbox: true
        //},
        {
            title: "名称",
            field: "name",
            sortable: true,
            formatter: function (value, row, index) {
                return '<a href="Edit?id=' + row.id + '">' + value + '</a>';
            }
        },
        {
            title: "创建时间",
            field: "createdDate",
            sortable: true,
        },
        {
            title: "实体",
            field: "entityName",
            sortable: false,
        },
        {
            title: "备注",
            field: "remark",
            sortable: true,
        },
        {
            title: "操作",
            field: "",
            sortable: false,
            formatter: function (value, row, index) {
                return "<a href='SetWorkflow?workflowId=" + row.id + "'>设置</a>";
            }
        }
    ];
</script>